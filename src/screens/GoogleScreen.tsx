import { View, Text } from 'react-native';
import React from 'react';

type Props = {};

const GoogleScreen = (props: Props) => {
  return (
    <View>
      <Text>GoogleScreen</Text>
    </View>
  );
};

export default GoogleScreen;
